<% user_reviews = current_user.user_reviews %>
<% user_services = current_user.services %>

<% if user_reviews.present? && current_user.provider == false %>
  <h3 class="h3-allivu mb-3"><strong>Reviews</strong></h3>

    <% user_reviews.each do |review| %>
    <div class="bezier card mb-3 ">
      <div class="card-header text-dark"><%= "#{review.booking.service.user.first_name} #{review.booking.service.user.last_name}" %>
      </div>
      <div class="card-body">
        <h5 class="card-title text-dark mt-0"><%= "#{review.rating.to_i} stars" %></h5>
        <p class="card-text text-dark"><%= review.comment %></p>
      </div>
    </div>
    <% end %>

  <% else %>

    <% if user_services.any? %>
      <% user_services.each do |service| %>
        <% if service.bookings.any? %>
          <% service.bookings.each do |booking| %>

            <% if booking.review.present? %>
              <div class="bezier card mb-3 ">
                <div class="card-header text-dark"><%= "#{booking.user.first_name} #{booking.user.last_name}" %></div>
                <div class="card-body pt-0">
                  <h5 class="card-title text-dark"><%= "#{booking.review.rating.to_i} stars" %></h5>
                  <p class="card-text text-dark"><%= booking.review.comment %></p>
                </div>
              </div>
            <% end %>

          <% end %>
        <% end %>
      <% end %>
    <% end %>

<% end %>
