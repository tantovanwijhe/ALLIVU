    <% if current_user.photo.key == nil %>
     <img class="avatar-large" <%= image_tag "profile.jpg" %>
    <% else %>
      <img class="avatar-large" <%= cl_image_tag current_user.photo.key %>
    <% end %>
